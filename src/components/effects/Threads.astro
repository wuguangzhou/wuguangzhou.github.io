---
// Threads — 半透明网格线纹理层，替代噪点纹理
---

<div class="threads-layer" aria-hidden="true"></div>

<style>
  .threads-layer {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    opacity: 0.04;
    background-image:
      repeating-linear-gradient(
        0deg,
        transparent,
        transparent 39px,
        var(--color-accent) 39px,
        var(--color-accent) 40px
      ),
      repeating-linear-gradient(
        90deg,
        transparent,
        transparent 39px,
        var(--color-accent) 39px,
        var(--color-accent) 40px
      );
    mask-image: radial-gradient(
      ellipse 80% 80% at 50% 50%,
      black 30%,
      transparent 70%
    );
    -webkit-mask-image: radial-gradient(
      ellipse 80% 80% at 50% 50%,
      black 30%,
      transparent 70%
    );
  }
</style>
